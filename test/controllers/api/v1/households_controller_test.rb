require "test_helper"

class HouseholdsControllerTest < ActionDispatch::IntegrationTest
  # setup do
  #   @household = households(:one)
  # end

  # test "should get index" do
  #   get households_url, as: :json
  #   assert_response :success
  # end

  # test "should create household" do
  #   assert_difference("Household.count") do
  #     post households_url, params: { household: { avatar_url: @household.avatar_url, last_hosted_iteration: @household.last_hosted_iteration, name: @household.name, pco_household: @household.pco_household, signed_up: @household.signed_up, times_hosted: @household.times_hosted, willing_to_host: @household.willing_to_host } }, as: :json
  #   end

  #   assert_response :created
  # end

  # test "should show household" do
  #   get household_url(@household), as: :json
  #   assert_response :success
  # end

  # test "should update household" do
  #   patch household_url(@household), params: { household: { avatar_url: @household.avatar_url, last_hosted_iteration: @household.last_hosted_iteration, name: @household.name, pco_household: @household.pco_household, signed_up: @household.signed_up, times_hosted: @household.times_hosted, willing_to_host: @household.willing_to_host } }, as: :json
  #   assert_response :success
  # end

  # test "should destroy household" do
  #   assert_difference("Household.count", -1) do
  #     delete household_url(@household), as: :json
  #   end

  #   assert_response :no_content
  # end
end
