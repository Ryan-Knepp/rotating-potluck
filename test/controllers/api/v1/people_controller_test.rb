require "test_helper"

class PeopleControllerTest < ActionDispatch::IntegrationTest
  # setup do
  #   @person = people(:one)
  # end

  # test "should get index" do
  #   get people_url, as: :json
  #   assert_response :success
  # end

  # test "should create person" do
  #   assert_difference("Person.count") do
  #     post people_url, params: { person: { address: @person.address, avatar_url: @person.avatar_url, email: @person.email, is_household_primary_contact: @person.is_household_primary_contact, is_organizer: @person.is_organizer, last_hosted_iteration: @person.last_hosted_iteration, name: @person.name, pco_person: @person.pco_person, phone_number: @person.phone_number, signed_up: @person.signed_up, times_hosted: @person.times_hosted, willing_to_host: @person.willing_to_host } }, as: :json
  #   end

  #   assert_response :created
  # end

  # test "should show person" do
  #   get person_url(@person), as: :json
  #   assert_response :success
  # end

  # test "should update person" do
  #   patch person_url(@person), params: { person: { address: @person.address, avatar_url: @person.avatar_url, email: @person.email, is_household_primary_contact: @person.is_household_primary_contact, is_organizer: @person.is_organizer, last_hosted_iteration: @person.last_hosted_iteration, name: @person.name, pco_person: @person.pco_person, phone_number: @person.phone_number, signed_up: @person.signed_up, times_hosted: @person.times_hosted, willing_to_host: @person.willing_to_host } }, as: :json
  #   assert_response :success
  # end

  # test "should destroy person" do
  #   assert_difference("Person.count", -1) do
  #     delete person_url(@person), as: :json
  #   end

  #   assert_response :no_content
  # end
end
